on_actions = {
	on_daily_WNG = {
		effect = {
			if = {
				limit = {
					has_government = neutrality
					has_completed_focus = WNG_the_royal_intervention
				}
				set_temp_variable = { WNG_neutral_support = WNG.party_popularity@neutrality }
				multiply_temp_variable = { WNG_neutral_support = 100 }
				set_variable = {
					var = WNG_popular_support_max
					value = WNG_neutral_support
				}
				if = {
					limit = {
						check_variable = {
							var = WNG_popular_support_max
							value = WNG_popular_support_min
							compare = less_than
						}
					}
					set_variable = {
						var = WNG_popular_support_max
						value = WNG_popular_support_min
					}
				}
				clamp_variable = {
					var = WNG_popular_support
					min = WNG_popular_support_min
					max = WNG_popular_support_max
				}
			}
		}
	}

	on_daily_YAN = {
		#Detecting if the number of folks on 1189, 1191, 1143, 1152, 1150, 1146, 1144, 1145, 1140, 1142, 1147 exceeds everywhere else. 
		effect = {
			if = {
				limit = {
					has_country_flag = YAN_insurgency_occured
					
				}

				YAN_pain_effect = yes 

				if = {
					limit = {
						check_variable = {
							var = YAN_max_deployable
							value = YAN_deployed_divisions
							compare = less_than
						}	
					}
					
					YAN = {
						country_event = { id = Indozeeb.2000 }
						set_country_flag = YAN_unrest_flag
					}
				}


			}
		}
	}
}