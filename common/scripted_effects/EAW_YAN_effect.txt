YAN_sistem_add = {
	add_to_variable = {
		var = YAN_sistem_support_kuda
		value = 3
	}
}

YAN_sistem_min = {
	subtract_from_variable = {
		var = YAN_sistem_support_kuda
		value = 3
	}
}

YAN_populace_add = {
	add_to_variable = {
		var = YAN_populace_support_kuda
		value = 3
	}
}

YAN_populace_min = {
	subtract_from_variable = {
		var = YAN_populace_support_kuda
		value = 3
	}
}

YAN_independence_add = {
	add_to_variable = {
		var = YAN_independence_support_kuda
		value = 3
	}
}

YAN_independence_min = {
	subtract_from_variable = {
		var = YAN_independence_support_kuda
		value = 3
	}
}


##For Future Refrence 

#populace angery: stability, factories shut down
#independence movement angery: stability, manpower, military factory bombed
#administration angery: pp gain debuffs, +consumer goods?

####Loterijen Banking Policy 

YAN_add_bank_effect = {
	if = {
		limit = { has_idea = YAN_addbank8 }
		add_political_power = 30
	}
	else_if = {
		limit = { has_idea = YAN_addbank7 }
		swap_ideas = {
			remove_idea = YAN_addbank7
			add_idea = YAN_addbank8
		}
	}
	else_if = {
		limit = { has_idea = YAN_addbank6 }
		swap_ideas = {
			remove_idea = YAN_addbank6
			add_idea = YAN_addbank7
		}
	}
	else_if = {
		limit = { has_idea = YAN_addbank5 }
		swap_ideas = {
			remove_idea = YAN_addbank5
			add_idea = YAN_addbank6
		}
	}
	else_if = {
		limit = { has_idea = YAN_addbank4 }
		swap_ideas = {
			remove_idea = YAN_addbank4
			add_idea = YAN_addbank5
		}
	}
	else_if = {
		limit = { has_idea = YAN_addbank3 }
		swap_ideas = {
			remove_idea = YAN_addbank3
			add_idea = YAN_addbank4
		}
	}
	else_if = {
		limit = { has_idea = YAN_addbank2 }
		swap_ideas = {
			remove_idea = YAN_addbank2
			add_idea = YAN_addbank3
		}
	}
	else_if = {
		limit = { has_idea = YAN_bank_idea }
		swap_ideas = {
			remove_idea = YAN_bank_idea
			add_idea = YAN_addbank2
		}
	}
	else = {
		add_ideas = YAN_bank_idea
	}
}

YAN_pain_effect = {
	every_unit_leader = {
		limit =  {
			is_leading_army = yes
		}

		set_variable = {
			var = YAN_total_units
			value = num_units_in_state@every_owned_state
		}

		set_temp_variable = { YANtemp1 = YAN_total_units } 
		divide_temp_variable = { YANtemp1 = 4 }

		set_variable = {
			var = YAN_max_deployable
			value = YANtemp1
		}

		##Figure out a way to collect the variable of units				 

		set_variable = {
			var = YAN_deployed_divisions
			value = YANtemp0
		}

		log = "[?YAN_total_units] [?YAN_max_deployable] [?YAN_deployed_divisions]"
	}
}